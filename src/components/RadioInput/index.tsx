import RadioInputProps from './RadioInput.props';
import * as Styled from './styles';

export default function RadioInput({ children }: RadioInputProps) {
  return <Styled.Container>{children}</Styled.Container>;
}
