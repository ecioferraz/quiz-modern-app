import styled, { css } from 'styled-components';

export const Container = styled.main`
  ${({ theme }) => css`
    button {
      background: #21BDCA;
      color: #FFF;
      margin-top: 9.8rem;
    }
  `}
`;
